@model WebApplication1.ViewModels.EmployeeListViewModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <form action="/employee/search" method="post">
        <input type="text" name="searchSrting" value=""/>
        <input type="submit" value="搜索"/>
    </form>

    <h2>@Model.UserName,@Model.Greeting</h2>
    <p><a href="/employee/addNew">新增</a></p>
    
    <div> 
        <ul>
            @foreach(var item in Model.EmloyeeViewList)
            {
               <li>@item.EmployeeName,@item.EmployeeSalary,@item.EmployeeCrade @*<a href="/employee/delete/@item.EmployeeId">删除</a>*@  
              @*<a href="/employee/Edit/@item.EmployeeId">编辑</a>*@

                   @Html.ActionLink("删除", "Delete", new { id = item.EmployeeId })
                   @Html.ActionLink("编辑", "Edit", new { id = item.EmployeeId })
            </li>
           
            }
        </ul>
    </div>
</body>
</html>
